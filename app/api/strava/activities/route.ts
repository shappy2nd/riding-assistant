import { NextResponse } from "next/server";
import { cookieName,fresh,seal,session } from "@/lib/strava";
export async function GET(){const old=await session();if(!old)return NextResponse.json({connected:false,activities:[]});try{const s=await fresh(old),r=await fetch("https://www.strava.com/api/v3/athlete/activities?per_page=100&page=1",{headers:{Authorization:`Bearer ${s.access_token}`}});if(!r.ok)throw new Error("fetch failed");const all=await r.json() as Array<Record<string,unknown>>,types=new Set(["Ride","MountainBikeRide","GravelRide","EBikeRide","VirtualRide","Handcycle"]),activities=all.filter(a=>types.has(String(a.sport_type||a.type))).slice(0,100);const res=NextResponse.json({connected:true,athlete:s.athlete,activities});if(s.refresh_token!==old.refresh_token)res.cookies.set(cookieName,await seal(s),{httpOnly:true,secure:true,sameSite:"lax",path:"/",maxAge:31536000});return res}catch{return NextResponse.json({connected:false,activities:[],error:"Strava 연결을 다시 진행해 주세요."},{status:401})}}
